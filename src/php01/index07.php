<?php
/*
$score1 = 100;
$score2 = 120;
$score3 = 50;

function test($score1, $score2, $score3){
    #$score1, $score2, $score3を$totalとかでまとめないとそれぞれの合計点が出せない
    if ($score1 + $score2 + $score3 > 210){
    #●●にちゃんと数字が入るように〇は書かない
    echo "合計点は〇〇なので合格です";
    }else{
        echo "合計点は〇〇なので不合格です";
    }
}
# test($score1, $score2, $score3);がないと呼び出せない

#三角形＝底辺×高さ÷２
#四角形＝長さ×長さ
#台形＝上底＋下底×高さ÷２

function three ($bottom,$height)
{
    $answer1 = $bottom * $height / 2;
    #計算した結果を $answer1 という変数に入れているが、returnしてない
    #計算結果を使えない（出力もできない）
}
function four($bottom, $height)
{
    $answer2 = $bottom * $height;
}
function trapezoid($up,$down,$height)
{
    $answer3 = ($up + $down) * $height / 2;
}

echo $answer1(4, 5);
echo $answer2(4, 5);
echo $answer3(6, 4, 5);
#ただの変数名（＄answer）を関数のように使おうとしているのでエラーがでる
*/
